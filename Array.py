matrix = [
    [0,1,1],
    [1,1,1],
    [0,0,0]
    ]
# This is called array
print(matrix[0][2])
# Array : means in python multi dimensinal is
# So for Access any items in the arry it would be like
# matrix [place in main lis][place in second list][place in n list]